# Run 001 Evidence

`dev/` is disposable harness-validation output and is not research evidence.

When a real run is preregistered, create a new run directory containing the exact config, boundary commit/binary hash, lab commit, environment manifest, attempt JSONL, verifier report, and a freeze manifest. Do not rewrite a frozen run.
